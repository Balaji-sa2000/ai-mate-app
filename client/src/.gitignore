# Ignore node_modules in both client and server
client/node_modules/
server/node_modules/

# Ignore build output folder (only for frontend)
client/build/

# Ignore environment variables file (contains sensitive API keys)
server/.env

# VS Code settings (optional)
.vscode/
